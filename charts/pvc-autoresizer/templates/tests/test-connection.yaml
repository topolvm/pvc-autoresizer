apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "pvc-autoresizer.fullname" . }}-test-connection"
  labels:
    {{- include "pvc-autoresizer.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "pvc-autoresizer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
